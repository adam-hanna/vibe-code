node_modules/
dist/
.vibe/
# Worktrees live inside the repo, so they must never be tracked by it.
.worktrees/
*.log
*.stackdump
# Per-machine Claude Code state - settings.local.json is the only thing in here.
# Shared agent config belongs in CLAUDE.md / AGENTS.md, which are tracked.
.claude/
