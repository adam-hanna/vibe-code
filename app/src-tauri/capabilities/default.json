{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window may do. Deliberately almost nothing: the three host commands are the app's entire capability surface, and every one of them is a message to a process this crate already started.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
